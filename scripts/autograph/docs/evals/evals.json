{
  "skill_name": "autograph",
  "evals": [
    {
      "id": 1,
      "prompt": "Need to create a CRM card for a new client — ACME Corp (consumer electronics), primary contact John Doe, CEO. We had a discovery call, they want a brand campaign in Eastern Europe for $15K. Create the card in the vault.",
      "expected_output": "Card written to work/crm/acme-corp.md with valid frontmatter (type: crm, status from schema enum, description not repeating title, 2-5 tags), only genuine existing neighbors in ## Related, indexed by the generated domain MOC, and touched to initialize recall metadata.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Check the vault health. If the score is below 90, show me what to fix. Repair any broken links you find. Show the tier distribution.",
      "expected_output": "Runs graph.py health, analyses metrics, fixes broken links when present, runs engine.py stats for tier distribution, runs moc.py generate if MOCs are stale, and runs engine.py decay to refresh relevance scores.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "Find everything in the vault related to Sample Inc — the client card, projects, linked contacts. Show backlinks, and report any orphans in the 'work' domain.",
      "expected_output": "Navigates via the generated work-domain MOC → wikilinks → work/crm/sample-inc.md. Uses graph.py backlinks for reverse lookups. Checks orphans in the 'work' domain via graph.py orphans. Regenerates MOCs and adds genuine sibling relations where useful.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "Update: we spoke with John Doe again — he left ACME Corp and is now CTO at Globex. Record it.",
      "expected_output": "Runs search.py (or grep) first, finds the existing John Doe card, performs SUPERSEDE — does NOT create a second card. Rewrites company/role in frontmatter and the top of the description to Globex/CTO (Compiled Truth), moves the old ACME/CEO values to an append-only ## History section as dated lines, sets updated:, and re-touches the card.",
      "files": []
    },
    {
      "id": 5,
      "prompt": "Process today's daily note into cards.",
      "expected_output": "Runs daily.py extract and supersede.py, reads schema node_types, lists the day's noteworthy items + topics. For each item runs a dedup-first lookup (search.py/grep) and applies ADD/UPDATE/SUPERSEDE/NOOP — updating existing cards instead of duplicating, resolving supersede-candidates.json. Adds only genuine existing neighbors, generates domain MOCs, and touches each card. Writes a daily-summary card only if the schema defines a summary type. Appends an <!-- autograph-processed --> marker to the end of the daily file (never edits existing lines) and reports created/updated/superseded counts.",
      "files": []
    }
  ]
}
